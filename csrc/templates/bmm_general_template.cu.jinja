// Auto-generated by OASR JIT -- {{ op_name }}
// General BMM instantiation grid for LayoutB={{ layout }}, Element={{ element }}.
//
// One translation unit per (B layout, dtype), because the grid is large and this
// is the *only* reason the seam exists: nvcc parallelizes across TUs but not
// within one, and the module's cold build is set by its biggest TU.  Measured on
// a 64-core box, RTX 5090: the whole `bmm` module builds in 112 s with the grid
// in one TU, 70 s split by layout, and 40 s split this way.
//
// The routing rule -- which alignment, which tile -- is in
// `include/oasr/gemm/bmm.cuh`, not here.  This file is only a point of
// instantiation.

#include <oasr/gemm/bmm.cuh>

namespace oasr {
namespace gemm {

GemmStatus {{ func_name }}(const GeneralBmmParams& p) {
    return detail::dispatchGeneralBmmLayout<{{ element }}, cutlass::layout::{{ layout }}>(p);
}

}  // namespace gemm
}  // namespace oasr
